{
  "name": "yew-ssr-v3",
  "version": "0.0.1",
  "scripts": {
    "cargo:run": "cargo run --features=ssr --bin simple_ssr -- --dir dist",
    "start": "trunk build index.html && npm run cargo:run",
    "build": "trunk build --release",
    "preview": "npm run build && npm run cargo:run",
    "generate:readme": "node generate-readme.js",
    "test:client": "lhci autorun"
  },
  "devDependencies": {
    "@lhci/cli": "0.12.0",
    "vite": "^4.1.0"
  }
}
